DATABASE_URL=postgres://postgres:postgres@localhost:5432/retro_db
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=retro_db
JWT_SECRET=your_super_secret_key_change_me
JWT_EXPIRES_IN=7d
PORT=3001
HOST=0.0.0.0
FRONTEND_URL=http://localhost:5173
# AUTO_MIGRATE=true   — явно включить миграции при старте
# AUTO_MIGRATE=false  — отключить (по умолчанию в dev выключено, в production включено)
